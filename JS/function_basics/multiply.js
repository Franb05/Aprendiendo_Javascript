/**
 * Escribe una función multiplyque tome 2 números y devuelva su producto.
 * multiply(3, 2)debería regresar 6
 */

function multiplyque(num1, num2) {
  let resultado = num1 * num2;
  return resultado;
}

console.log(multiplyque(2, 3));
